import React from 'react';

import '../style.css';

export default () => (
  <div className="container max-w-2xl px-4 mx-auto text-gray-900 flex flex-col min-h-screen">
    <p className="my-4">Hmm, not sure if I have thought about that... 🤔</p>
    <p className="text-sm m-0">
      Let's get you back{' '}
      <a href="/">where you started</a>
    </p>
  </div>
);
